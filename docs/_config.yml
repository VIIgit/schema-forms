title: ""
description: "A HAL-Form extension with JSON Validation Schema you will love"
show_downloads: false
theme: pmarsceill/just-the-docs

# pmarsceill/just-the-docs

baseurl: "/" # the subpath of your site, e.g. /blog
#url: "https://pmarsceill.github.io" # the base hostname & protocol for your site, e.g. http://example.com

permalink: pretty
exclude: ["node_modules/", "*.gemspec", "*.gem", "Gemfile", "Gemfile.lock", "package.json", "package-lock.json",  "script/", "LICENSE.txt", "lib/", "bin/", "README.md", "Rakefile"]

# Set a path/url to a logo that will be displayed instead of the title
#logo: "/assets/images/just-the-docs.png"

# Enable or disable the site search
search_enabled: true

# Set the search token separator for hyphenated-word search:
search_tokenizer_separator: /[\s/]+/

# Enable or disable heading anchors
heading_anchors: true

# Aux links for the upper right navigation
aux_links:
  "Schema Forms on GitHub":
    - "//github.com/VIIgit/schema-forms"

# Footer content appears at the bottom of every page's main content
#footer_content: "Copyright &copy; 2020"

# Color scheme currently only supports "dark" or nil (default)
color_scheme: "dark"

# Google Analytics Tracking (optional)
# e.g, UA-1234567-89
# ga_tracking: 

plugins:
  - jekyll-seo-tag

compress_html:
  clippings: all
  comments: all
  endings: all
  startings: []
  blanklines: false
  profile: false


# other
x-theme: jekyll-theme-slate